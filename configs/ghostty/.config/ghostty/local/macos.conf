font-size = 15
window-width = 160
window-height = 48

# Auto-start tmux: attach to `main` if it exists, else create it.
# Lives here (terminal config), NOT .zshrc — a tmux failure can't brick the shell.
command = /opt/homebrew/bin/tmux new-session -A -s main
