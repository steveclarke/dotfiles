font-family = JetBrains Mono NF
font-size = 11

theme = Dracula+

# --------------
# Keybinds
# --------------

# Split navigation
keybind = shift+alt+left=goto_split:left
keybind = shift+alt+h=goto_split:left
keybind = shift+alt+right=goto_split:right
keybind = shift+alt+l=goto_split:right
keybind = shift+alt+down=goto_split:down
keybind = shift+alt+j=goto_split:down
keybind = shift+alt+up=goto_split:up
keybind = shift+alt+k=goto_split:up

# Split manipulation
keybind = shift+alt+d=new_split:down
keybind = shift+alt+u=new_split:up
keybind = shift+alt+]=new_split:right
keybind = shift+alt+[=new_split:left

keybind = shift+alt+t=new_tab

keybind = ctrl+enter=toggle_maximize

